# multi-media-stego-app
A Python-based multimedia steganography desktop app that hides secret messages inside images (PNG/BMP), audio (WAV), video (AVI), and text files using LSB encoding. Built with CustomTkinter for a modern dark-mode GUI, it supports both encoding and decoding with an intuitive tabbed interface.
